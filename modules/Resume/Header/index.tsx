import { Box } from "@chakra-ui/layout";
import React from "react";

interface Props {}

const Header = (props: Props) => {
  return <Box aria-label="Header Layout" w="inherit"></Box>;
};

export default Header;
